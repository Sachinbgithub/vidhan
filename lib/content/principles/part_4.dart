import 'package:flutter/material.dart';

class Part_4 extends StatelessWidget {
  const Part_4({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("this is part IV")),
    );
  }
}
