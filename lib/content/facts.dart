import 'package:flutter/material.dart';

class Facts extends StatelessWidget {
  const Facts({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("this is fact")),
    );
  }
}
